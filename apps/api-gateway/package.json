{
  "name": "@nexus/api-gateway",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nexus/shared": "workspace:*",
    "@nexus/infra": "workspace:*",
    "@nexus/control-plane": "workspace:*",
    "@nexus/tool-gateway": "workspace:*",
    "@nexus/kernel": "workspace:*",
    "@nexus/memory": "workspace:*",
    "@nexus/observability": "workspace:*",
    "@nexus/phase-intent": "workspace:*",
    "@nexus/providers": "workspace:*",
    "@nexus/guardrails": "workspace:*",
    "fastify": "^5.0.0",
    "@fastify/websocket": "^11.0.0",
    "@fastify/cors": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.0.0",
    "rimraf": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
